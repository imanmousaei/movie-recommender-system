import gui


if __name__ == '__main__':
    # df = pd.read_csv('cleaned-dataset/pca_500.csv')
    # prepare_clustering_algorithms()

    # movie_id = 'imdb_id'
    # recom_ids = get_clustering_recommendations(movie_id, 'kmeans', num_recommendations=5)
    # print('recom_ids', recom_ids)

    gui.build_gui()
